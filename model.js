//const colourSet = require('./data')
//your data is using this model. no need for a circular include (google it)

class primary {
    constructor(id, colour, ) {
        this.id = id
        this.colour = colour
    }
}


module.exports = {
    primary
}
